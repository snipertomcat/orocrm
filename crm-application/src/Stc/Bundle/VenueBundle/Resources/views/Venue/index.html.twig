{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set gridName = 'stc_venue_grid' %}

{% set pageTitle = 'stc.venue.entity_plural_label'|trans %}

{% block navButtons %}
    {% if resource_granted('stc_venue_create') %}
        {{ UI.addButton({
        'path' : path('stc_venue_create'),
        'entity_label': 'stc.venue.entity.label'|trans
        }) }}
    {% endif %}
{% endblock %}